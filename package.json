{
  "name": "fast-copy",
  "version": "1.4.0",
  "private": true,
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.304",
    "typescript": "^5.8.3",
    "vite": "^6.3.2"
  }
}
